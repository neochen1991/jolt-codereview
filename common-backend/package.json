{
  "name": "@jolt/common-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jolt public platform backend for users, permissions, system settings, and model management.",
  "scripts": {
    "clean": "node -e \"import('node:fs').then(({ rmSync }) => rmSync('build', { recursive: true, force: true }))\"",
    "build": "npm run clean && tsc -p tsconfig.backend.json",
    "dev": "npm run build && node build/backend/common-server.js",
    "start": "node build/backend/common-server.js",
    "verify": "npm run build"
  },
  "dependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
